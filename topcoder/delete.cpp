#include<cstdio>
#include<cmath>

using namespace std;

#define REP(i,j,k) for(int (i)=(j); (i)<(k); ++(i))
#define FILL(n,v) memset(n, v, sizeof((n)))

int main(){
	
	int in; cin >> in;
	
	REP(cn,0,1){
		
		REP(i,0,n){
			cout << in << endl;
		}
		REP(j,0,m){
			
		}
		
	}
	
	return 0;
}